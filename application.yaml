apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portal-glow
  namespace: argocd
  annotations:
      release.tag: "08d076f_21627643912"
spec:
  project: default
  source:
    repoURL: https://github.com/vkirankumar/portal-glow-mock-helm
    targetRevision: HEAD
    path: .
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/template/metadata/annotations/release.tag
